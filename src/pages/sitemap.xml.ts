import {getPosts} from '../data/articles';export async function GET(){const paths=['/','/blog/','/projects/','/tools/','/about/',...(await getPosts()).map(p=>'/'+p.slug+'/')];return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map(p=>`<url><loc>${new URL(p,'https://cdsee045.github.io').href}</loc></url>`).join('')}</urlset>`,{headers:{'Content-Type':'application/xml; charset=utf-8'}})}
