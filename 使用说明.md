# 个人网站使用说明

网站地址：<https://cdsee045.github.io/>

源码仓库：<https://github.com/cdsee045/cdsee045.github.io>

网站使用 Astro 构建。每次向 `main` 分支提交修改后，GitHub Actions 会自动检查、构建并发布，一般一两分钟后可以在线查看。

## 最省事的修改方式：直接使用 GitHub 网页

适合修改文字、项目、链接和工具导航。

1. 打开源码仓库，进入需要修改的文件。
2. 点击右上角铅笔图标 **Edit this file**。
3. 修改内容，点击 **Commit changes**。
4. 提交到 `main` 分支。
5. 打开仓库的 **Actions** 页面，等待 `Publish personal website` 显示绿色对勾。
6. 刷新 <https://cdsee045.github.io/> 查看结果。如果仍显示旧内容，稍等片刻后强制刷新浏览器。

网页编辑没有本地预览，改动较大时建议使用下面的本地方式。

## 在电脑上修改和预览

需要安装 Git 和 Node.js 24。

第一次使用：

```bash
git clone git@github.com:cdsee045/cdsee045.github.io.git
cd cdsee045.github.io
npm ci
npm run dev
```

浏览器打开终端显示的地址，通常是 <http://localhost:4321/>。保存文件后，预览页面会自动更新。

请在上面克隆得到的 `cdsee045.github.io` 目录中执行 Git 提交和推送。之前用于制作预览的“个人主页展示”目录不是这份远程仓库的完整克隆，不能假定在那里直接执行 `git push` 就能同步。

以后继续修改：

```bash
cd cdsee045.github.io
git pull
npm ci
npm run dev
```

## 修改首页内容

首页的自我介绍、个人经历和代表项目在：

`src/data/profile.js`

首页默认英文，右上角可以切换中文，并记住语言选择。首页的暗色样式在 `src/styles/portfolio.css` 中独立维护。

`name`、`avatar`、`email`、`github` 和 `bilibili` 分别设置姓名、头像及联系方式。其他文字使用 `en`（英文）和 `zh`（中文）两份内容，例如：

```js
introduction: {
  en: "Hi, I’m Weijian Li. I am learning Blender rendering.",
  zh: "你好，我是李威检，正在学习 Blender 渲染。",
},
```

教育经历在 `education` 数组中维护，学校 `school`、专业学位 `degree`、时间 `period`、状态 `status` 都有 `en` 和 `zh` 字段。北航的开始时间为 2027 年，目前标注预计入学，入学后请更新名片 `role`、`affiliation` 及教育状态。

代表项目在 `featuredProjects` 中维护，单项格式如下：

```js
{
  name: { en: 'Project name', zh: '项目名称' },
  description: { en: 'My contribution and results.', zh: '我的工作和成果。' },
  url: 'https://github.com/cdsee045/仓库名',
},
```

其他履历在 `experiences` 数组中维护，使用同样的双语 `name` 和 `description` 字段。数组为空时，首页显示 “To be added.” / “待补充。”。

首页代表项目和“项目”页面分别维护：后者使用 `src/data/site.js` 中的 `projects`。博客、工具导航等内页继续使用原来的布局。

头像可用新图片覆盖 `public/medias/avatar.jpg`，或修改 `profile.js` 中的 `avatar` 地址。Bilibili 目前链接到网站首页，有个人空间地址后请更新 `bilibili` 字段。

## 修改项目页和工具导航

项目和工具都在：

`src/data/site.js`

项目格式：

```js
{
  name: '项目名称',
  tag: '项目类型',
  description: '简短介绍',
  url: 'https://github.com/cdsee045/仓库名'
}
```

工具格式：

```js
['工具名称', '分类名称', '工具用途', 'https://工具地址/']
```

使用相同的分类名称，工具页会自动生成分类筛选按钮。每个工具显示为直达官网的卡片，可以按名称、用途和分类搜索。

编辑时请使用英文标点：条目之间使用英文逗号 `,`，不要使用中文逗号 `，`。注释使用 `// 注释内容`，不要使用反斜杠。文字中如果有英文单引号，可以用双引号包住整段文字，例如 `"Hi, I'm LiWeijian"`。

发布前可运行 `node --check src/data/site.js` 和 `node --check src/data/profile.js` 检查格式，再运行 `npm run build`。上传成功但 Actions 构建失败时，线上仍然是上一次成功发布的版本；请打开失败任务查看错误文件和行号。

## 修改关于页

关于页在：

`src/pages/about.astro`

普通文字可以直接修改。链接格式为：

```html
<a href="https://example.com">链接名称</a>
```

## 发布新文章

1. 复制 `templates/new-post.md`。
2. 将副本放入 `src/content/posts/`。
3. 文件名建议使用英文，例如 `ros2-first-node.md`。
4. 填写文章信息和正文。
5. 完成后将 `draft: true` 改为 `draft: false`。

文章示例：

```markdown
---
title: "ROS 2 节点入门"
slug: "ros2-first-node"
date: "2026-09-25"
category: "ROS"
description: "用 Python 创建第一个 ROS 2 节点。"
draft: false
---

## 准备工作

从这里开始写正文。
```

注意事项：

- `slug` 使用小写英文、数字和连字符，并且不能与其他文章重复。
- 日期格式为 `年-月-日`。
- `draft: true` 的文章不会发布。
- 发布后的地址是 `https://cdsee045.github.io/blog/slug/`。
- 新文章会自动加入博客列表、搜索、RSS 和 sitemap。
- 首页不会显示最新文章，这是当前首页的设计。
- 新文章直接放在 `src/content/posts/` 下；当前读取规则不包含其子目录。
- 如果仓库中看不到 `src/content/posts/`（Git 不保存空目录），可在 GitHub 的 **Add file → Create new file** 中输入完整路径 `src/content/posts/ros2-first-node.md` 创建。

## 在文章中添加图片

将图片放入 `public/images/`，例如：

`public/images/ros2-node.png`

在 Markdown 中引用：

```markdown
![ROS 2 节点示意图](/images/ros2-node.png)
```

图片文件名建议使用英文，不要包含空格。

## 修改颜色和布局

- 全站基础排版：`src/styles/global.css`
- 当前方案 F 的页面风格：`src/styles/hybrid.css`
- 页头、导航和页脚：`src/layouts/Layout.astro`
- 首页结构：`src/pages/index.astro`
- 博客列表：`src/pages/blog.astro`
- 工具导航：`src/pages/tools.astro`

修改样式前建议先在本地运行 `npm run dev`，同时观察电脑和手机宽度下的效果。

## 发布前检查

```bash
npm run build
npm run check:links
```

两个命令都成功后再提交：

```bash
git add .
git commit -m "更新个人网站内容"
git push origin main
```

推送完成后，GitHub Actions 会自动发布。无需上传 `dist`、`node_modules` 或 `.astro` 文件夹。

## 查看发布状态

打开：<https://github.com/cdsee045/cdsee045.github.io/actions>

- 黄色圆点：正在构建或发布。
- 绿色对勾：发布成功。
- 红色叉号：构建失败，点击该任务可以查看错误。

## 恢复旧版本

本次重建前的网站保存在远程分支：

`codex/pre-astro-backup-20260925`

普通内容改错时，可以重新编辑文件并提交纠正。需要撤销完整提交时，在本地克隆目录执行以下命令（先确认没有未提交的修改）：

```bash
git pull --ff-only
git log --oneline -5
# 将下面的 abc1234 换成实际要撤销的提交编号
git revert abc1234
git push origin main
```

这会创建一条撤销提交，保留历史。若提示冲突，应处理冲突再提交；不要强制覆盖 `main`。备份分支保留的是旧 Hexo 成品文件，完整恢复旧站还涉及 Pages 发布方式调整，不能只切换本地分支就认为已恢复上线。

## 不建议直接修改的文件

- `src/data/posts.json`：从旧 Hexo 站点恢复的文章正文。
- `src/data/legacy-routes.json`：旧网址兼容规则。
- `migration-report.json`：旧文章迁移记录。
- `package-lock.json`：依赖版本锁定文件，安装依赖时由 npm 管理。
- `dist/`：自动生成的构建结果。

## GitHub Profile

Profile 文稿保存在 `profile/README.md`。要让它显示在 GitHub 个人主页，需要创建公开仓库 `cdsee045/cdsee045`，然后把该文件作为仓库根目录的 `README.md` 提交。
