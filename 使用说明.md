# 个人网站使用说明

网站地址：<https://cdsee045.github.io/>

源码仓库：<https://github.com/cdsee045/cdsee045.github.io>

网站使用 Astro 构建。每次向 `main` 分支提交修改后，GitHub Actions 会自动检查、构建并发布，一般一两分钟后可以在线查看。

## 最省事的修改方式：直接使用 GitHub 网页

适合修改文字、项目、链接和工具导航。

1. 打开源码仓库，进入需要修改的文件。
2. 点击右上角铅笔图标 **Edit this file**。
3. 修改内容，点击 **Commit changes**。
4. 提交到 `main` 分支。
5. 打开仓库的 **Actions** 页面，等待 `Publish personal website` 显示绿色对勾。
6. 刷新 <https://cdsee045.github.io/> 查看结果。如果仍显示旧内容，稍等片刻后强制刷新浏览器。

网页编辑没有本地预览，改动较大时建议使用下面的本地方式。

## 在电脑上修改和预览

需要安装 Git 和 Node.js 24。

第一次使用：

```bash
git clone git@github.com:cdsee045/cdsee045.github.io.git
cd cdsee045.github.io
npm ci
npm run dev
```

浏览器打开终端显示的地址，通常是 <http://localhost:4321/>。保存文件后，预览页面会自动更新。

以后继续修改：

```bash
cd cdsee045.github.io
git pull
npm ci
npm run dev
```

## 修改首页内容

首页的自我介绍、个人经历和代表项目在：

`src/data/profile.js`

示例：

```js
export const profile = {
  introduction: '在这里填写你的自我介绍。',
  experiences: [
    {
      period: '2024 — 至今',
      title: '学校或公司名称',
      description: '专业、职位或主要经历。',
    },
  ],
  featuredProjects: [
    {
      name: '项目名称',
      description: '项目解决了什么问题，你负责了什么。',
      url: 'https://github.com/cdsee045/仓库名',
    },
  ],
};
```

可以添加多个经历或项目，每一项之间用英文逗号分隔。数组为空时，首页显示“待补充”。

## 修改项目页和工具导航

项目和工具都在：

`src/data/site.js`

项目格式：

```js
{
  name: '项目名称',
  tag: '项目类型',
  description: '简短介绍',
  url: 'https://github.com/cdsee045/仓库名'
}
```

工具格式：

```js
['工具名称', '分类名称', '工具用途', 'https://工具地址/']
```

使用相同的分类名称，工具页会自动将它们放进同一个分组。

## 修改关于页

关于页在：

`src/pages/about.astro`

普通文字可以直接修改。链接格式为：

```html
<a href="https://example.com">链接名称</a>
```

## 发布新文章

1. 复制 `templates/new-post.md`。
2. 将副本放入 `src/content/posts/`。
3. 文件名建议使用英文，例如 `ros2-first-node.md`。
4. 填写文章信息和正文。
5. 完成后将 `draft: true` 改为 `draft: false`。

文章示例：

```markdown
---
title: "ROS 2 节点入门"
slug: "ros2-first-node"
date: "2026-09-25"
category: "ROS"
description: "用 Python 创建第一个 ROS 2 节点。"
draft: false
---

## 准备工作

从这里开始写正文。
```

注意事项：

- `slug` 使用小写英文、数字和连字符，并且不能与其他文章重复。
- 日期格式为 `年-月-日`。
- `draft: true` 的文章不会发布。
- 发布后的地址是 `https://cdsee045.github.io/blog/slug/`。
- 新文章会自动加入博客列表、搜索、RSS 和 sitemap。

## 在文章中添加图片

将图片放入 `public/images/`，例如：

`public/images/ros2-node.png`

在 Markdown 中引用：

```markdown
![ROS 2 节点示意图](/images/ros2-node.png)
```

图片文件名建议使用英文，不要包含空格。

## 修改颜色和布局

- 全站基础排版：`src/styles/global.css`
- 当前方案 F 的页面风格：`src/styles/hybrid.css`
- 页头、导航和页脚：`src/layouts/Layout.astro`
- 首页结构：`src/pages/index.astro`
- 博客列表：`src/pages/blog.astro`
- 工具导航：`src/pages/tools.astro`

修改样式前建议先在本地运行 `npm run dev`，同时观察电脑和手机宽度下的效果。

## 发布前检查

```bash
npm run build
npm run check:links
```

两个命令都成功后再提交：

```bash
git add .
git commit -m "更新个人网站内容"
git push origin main
```

推送完成后，GitHub Actions 会自动发布。无需上传 `dist`、`node_modules` 或 `.astro` 文件夹。

## 查看发布状态

打开：<https://github.com/cdsee045/cdsee045.github.io/actions>

- 黄色圆点：正在构建或发布。
- 绿色对勾：发布成功。
- 红色叉号：构建失败，点击该任务可以查看错误。

## 恢复旧版本

本次重建前的网站保存在远程分支：

`codex/pre-astro-backup-20260925`

普通内容改错时，优先在 GitHub 的提交历史中使用 **Revert** 撤销对应提交。不要使用强制推送覆盖 `main`。

## 不建议直接修改的文件

- `src/data/posts.json`：从旧 Hexo 站点恢复的文章正文。
- `src/data/legacy-routes.json`：旧网址兼容规则。
- `migration-report.json`：旧文章迁移记录。
- `package-lock.json`：依赖版本锁定文件，安装依赖时由 npm 管理。
- `dist/`：自动生成的构建结果。

## GitHub Profile

Profile 文稿保存在 `profile/README.md`。要让它显示在 GitHub 个人主页，需要创建公开仓库 `cdsee045/cdsee045`，然后把该文件作为仓库根目录的 `README.md` 提交。
